// switch is to perform different action based on different condition

let x= 0;
switch(x) {
    case 0:
        text = "off";
        break;
    case 1:
        text = "On";
        break;
    default:
        text = "No break";
}
console.log("value od Text:",text);